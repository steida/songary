.song
  pos absolute 0 0 0 0

  article
    // NOTE(steida): With display inline-block we can measure actual size.
    display inline-block
    padding 1em
    text-align left
    white-space pre

    sup
      color #428BCA
      font-size 110%
      font-style italic
      font-weight bold
      letter-spacing 1px

  menu
    inline-block-container()
    background-color rgba(0, 0, 0, 0.7)
    border-radius 1em
    font-size 2rem
    margin 0
    padding 0
    // NOTE(steida): Consider fixed position, but then ViewportClientPosition
    // must be replaced for something else.
    position absolute
    // transition all .5s

    &.hidden
      background-color rgba(0, 0, 0, 0)

    a, menuitem
      // NOTE(steida): Override bootstrap.
      color #fff !important
      margin 0
      padding .5em .625em

    menuitem
      cursor pointer
      font-weight bold

      /.is-desktop &:hover
      &.touch-hover
        text-decoration underline