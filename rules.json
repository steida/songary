{
  "rules": {
    "songs": {
      ".read": true,
      ".indexOn": ["createdBy"],
      "$id": {
        ".write": "auth != null"
      }
    },
    "users": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
