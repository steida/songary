{
  "rules": {
    "songs": {
      ".read": true,
      "$id": {
        ".write": "auth != null"
      }
    },
    "users": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
